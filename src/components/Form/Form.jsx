import React from "react";
import './Form.css';
import { useTelegram } from "../../hooks/useTelegram";

const Form = () => {
    const {user, onClose} = useTelegram();

    return (
       <div>
       
       </div>
    );
};

export default Form;